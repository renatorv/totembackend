------------------------------------------------------------------------------------------------------------------------
AULA 7
Variaveis de ambiente => 44 minutos

------------------------------------------------------------------------------------------------------------------------

AULA 5 - AUTENTICAÇÃO

Padrão OAUTH2 - LOGIN COM SENHA

Passa-se usuário e senha e é gerado um token que é trafegado.

Criando a migration:
alembic revision --autogenerate -m "Add hashed_password field"
alembic upgrade head

pip install "passlib[bcrypt]"

pip install pyjwt

Erro que ocorreu durante a aula 1: 18
Not enough segments => tem um padrão para retornar o access_token em /login (routes/auth.py)



------------------------------------------------------------------------------------------------------------------------

CONCEITO INICIAL DE MIGRATIONS:
Aula 4 3 minutos

pip install alembic

cd src

alembic init alembic

alembic revision --autogenerate -m "Created location column on Store and removed enabled"

alembic upgrade head

PROBLEMA DE INTEGRIDADE: 27 MINUTOS

RELAÇÕES ENTRE TABELAS: 40 MINUTOS

pip freeze => requirements.txt
pip install -r .\requirements.txt

------------------------------------------------------------------------------------------------------------------------

INJEÇÃO DE DEPENDÊNCIAS:
Conceito no final da aula 2